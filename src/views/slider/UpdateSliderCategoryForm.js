import React from 'react';

const UpdateSliderCategoryForm = ({ handleEditFormSubmit, initialData = {}, onClose, sendResponse }) =>
  
  console.log(initialData,handleEditFormSubmit)
  {
  return (
    <div>
      
    </div>
  );
}

export default UpdateSliderCategoryForm;
